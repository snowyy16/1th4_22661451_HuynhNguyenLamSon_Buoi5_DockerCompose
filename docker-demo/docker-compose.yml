services:
  web:
    image: node:18-alpine
    working_dir: /app
    volumes:
      - .:/app
    command: sh -c "npm install && node app.js"
    ports:
      - "5000:3000"
  
  redis:
    image: redis:alpine